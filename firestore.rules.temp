rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all reads/writes for authenticated users
    // This is ONLY for debugging - will be replaced with proper rules once login works
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
